node_modules/
.DS_Store

# Built blueprints are artifacts — except the ones in examples/, which are
# committed on purpose so the repo can be browsed without running anything.
*.blueprint.html
!examples/*.blueprint.html
